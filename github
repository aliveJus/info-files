git clone  https://github.com/aliveJus/riocamp.git  // клонируем наш репризиторий в текущую папку

git status // проверка новых файлов/изменений

git add index.php  // добавляем новый файл

git commit -m "тут просто пишем комментарий, например Add index.php" // и еще раз зачем то

git push   // финальная отправка

git pull // скачать (получить) с сервера то, что не хватает

git add -A  // закачать на сервер все файлы

git commit -m "тут просто пишем комментарий"

если забыл написать комментарий, то ESC , пишем двоеточие + WQ   (:wq)